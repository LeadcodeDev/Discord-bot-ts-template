import { EventInterface } from 'Core/interfaces'
import { Event } from 'Core/interfaces/decorators'
import { Events } from 'Core/api'

@Event({ type: Events.EXAMPLE })
class ~Example implements EventInterface {
	public async run(): Promise<void> {
		// Your code here
	}
}

export default new ~Example()
